package main

import ()

func main() {
	/*
	  cmd := &cobra.Command{
	      Short: "Zeus is a deployment CLI tool for enviroments with only one machine.",
	      CompletionOptions: cobra.CompletionOptions{
	        DisableDefaultCmd: true,
	      },
	  }

	  conn, err := connhelper.GetConnectionHelper("ssh://user@ip")
	  if err != nil {
	    panic(err)
	  }

	  cli, err := client.NewClientWithOpts(
	    client.WithDialContext(conn.Dialer),
	    client.WithAPIVersionNegotiation(),
	  )
		if err != nil {
			panic(err)
		}
		defer cli.Close()

	  cmd.AddCommand(
	    deploy.NewDeployCommand(cli),
	    network.NewNetworkCommand(cli),
	  )
	  cobra.CheckErr(cmd.Execute())
	*/
}
